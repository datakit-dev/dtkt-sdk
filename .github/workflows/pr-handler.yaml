name: PR Handler

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  pr-handler:
    permissions:
      contents: read
      pull-requests: write

    uses: datakit-dev/.github/.github/workflows/pull-request-handler.yaml@main
    with:
      labeler-config-path: .github/labeler.yaml
