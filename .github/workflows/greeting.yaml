name: Greetings

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  greetings:
    permissions:
      issues: write
      pull-requests: write

    uses: datakit-dev/.github/.github/workflows/greetings.yaml@main
