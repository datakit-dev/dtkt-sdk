apiVersion: v1beta2
kind: Flow
spec:
  name: nc_interaction_stop
  inputs:
    - id: x
      int64: {}
  interactions:
    - id: confirm
      when: "= inputs.x.value > 0"
      node_control:
        stop_when: "= true"
  outputs:
    - id: result
      value: "= interactions.confirm.value"
