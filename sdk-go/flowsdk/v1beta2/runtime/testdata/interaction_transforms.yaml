apiVersion: v1beta2
kind: Flow
spec:
  name: interaction_transforms
  inputs:
    - id: x
      int64: {}
  interactions:
    - id: ask
      when: "= inputs.x.value > 0"
      transforms:
        - map: "= this.value * 10"
  outputs:
    - id: result
      value: "= interactions.ask.value"
