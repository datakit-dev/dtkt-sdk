apiVersion: v1beta2
kind: Flow
spec:
  name: input_bool_filter_true
  description: "Bool input with filter transform keeping only true values"
  inputs:
    - id: x
      bool: {}
      transforms:
        - filter: "= this.value == true"
  outputs:
    - id: result
      value: "= inputs.x.value"
