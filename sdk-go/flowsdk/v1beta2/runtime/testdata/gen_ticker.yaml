apiVersion: v1beta2
kind: Flow
spec:
  name: gen_ticker
  generators:
    - id: tick
      ticker:
        interval: "0.01s"
  outputs:
    - id: result
      value: "= generators.tick.value"
      flow_control:
        stop_when: "= int(generators.tick.eval_count) >= 5"
