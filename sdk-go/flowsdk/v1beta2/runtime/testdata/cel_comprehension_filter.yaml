apiVersion: v1beta2
kind: Flow
spec:
  name: cel_comprehension_filter
  description: "filter() macro projecting a sublist of names where n > 1"
  inputs:
    - id: x
      string: {}
  outputs:
    - id: result
      value: "= inputs.x.value.items.filter(i, i.n > 1).map(i, i.name)"
