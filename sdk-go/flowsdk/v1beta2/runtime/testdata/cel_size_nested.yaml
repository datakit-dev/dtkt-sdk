apiVersion: v1beta2
kind: Flow
spec:
  name: cel_size_nested
  description: "Compute size() of a list nested inside a struct"
  inputs:
    - id: x
      string: {}
  outputs:
    - id: result
      value: "= size(inputs.x.value.tags)"
