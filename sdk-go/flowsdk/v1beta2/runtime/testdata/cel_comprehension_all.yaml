apiVersion: v1beta2
kind: Flow
spec:
  name: cel_comprehension_all
  description: "all() macro over a list of structs nested inside an input value"
  inputs:
    - id: x
      string: {}
  outputs:
    - id: result
      value: "= inputs.x.value.items.all(i, i.n > 0)"
