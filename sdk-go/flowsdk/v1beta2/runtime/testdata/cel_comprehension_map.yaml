apiVersion: v1beta2
kind: Flow
spec:
  name: cel_comprehension_map
  description: "map() macro doubling the n field of each element"
  inputs:
    - id: x
      string: {}
  outputs:
    - id: result
      value: "= inputs.x.value.items.map(i, i.n * 2)"
