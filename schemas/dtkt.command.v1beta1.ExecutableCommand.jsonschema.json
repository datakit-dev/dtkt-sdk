{
  "$id": "dtkt.command.v1beta1.ExecutableCommand.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A command to execute.",
  "patternProperties": {
    "^(expects_stdin)$": {
      "default": false,
      "description": "Indicates whether this command expects stdin input.\n When false, stdin is not opened (command has no stdin).\n When true, stdin is opened and remains available for streaming input until EOF is explicitly sent.\n Default: false (most commands don't need stdin)",
      "type": "boolean"
    }
  },
  "properties": {
    "args": {
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "command": {
      "description": "Command executable.",
      "maxLength": 255,
      "minLength": 1,
      "type": "string"
    },
    "env": {
      "additionalProperties": {
        "type": "string"
      },
      "propertyNames": {
        "type": "string"
      },
      "type": "object"
    },
    "expectsStdin": {
      "default": false,
      "description": "Indicates whether this command expects stdin input.\n When false, stdin is not opened (command has no stdin).\n When true, stdin is opened and remains available for streaming input until EOF is explicitly sent.\n Default: false (most commands don't need stdin)",
      "type": "boolean"
    },
    "timeout": {
      "$ref": "google.protobuf.Duration.jsonschema.json"
    },
    "workdir": {
      "default": "",
      "type": "string"
    }
  },
  "required": [
    "command"
  ],
  "title": "Executable Command",
  "type": "object"
}
